<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/indexStyle.css">
</head>
<body>
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <h1>Hello, <%- name %></h1>
    <header>
        <h2 class="logo">Logo</h2>
        <nav class="navigation">
            <form id="falloutDice-button" action="/falloutDice" method="GET">
                <button type="submit">Fallout Dice Roller</button>
            </form>
            <form id="falloutCM-button" action="/falloutCM" method="GET">
                <button type="submit">Fallout Character Manager</button>
            </form>
            <form id="logout-button" action="/logout" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit">Log Out</button>
            </form>
        </nav>
    </header>
</body>